using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PathFinding : MonoBehaviour {

    List<GameObject> open = new List<GameObject>(); //nodes that are getting evaluated
    List<GameObject> closed = new List<GameObject>(); //nodes that have already been evaluated

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
